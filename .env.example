# Core
AIPO_OUTPUT_DIR=out
AIPO_LOG_LEVEL=INFO
AIPO_SEED=42

# Adapter defaults (placeholders only; set real values per environment)
GARAK_MODEL=openai:gpt-4o-mini
ART_ENABLED=true

# API keys should NOT be committed. Use a secrets manager in CI.
